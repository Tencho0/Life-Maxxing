// Animation duration for the LifeMaxxing fl_chart wrappers. Mutable so the test
// environment can set it to Duration.zero for deterministic, settle-able frames
// (see test/support/test_env.dart). Defaults to fl_chart's normal feel.
Duration lmChartAnimationDuration = const Duration(milliseconds: 150);
